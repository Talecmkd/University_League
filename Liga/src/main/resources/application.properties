spring.application.name=Liga

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/university_league
spring.datasource.username=liga_user
spring.datasource.password=Liga123!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=9090

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# H2 Database Configuration (DISABLED - keeping for reference)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.h2.console.path=/h2
# spring.h2.console.enabled=true

# Azure PostgreSQL Configuration (DISABLED - keeping for reference)
# spring.datasource.url=jdbc:postgresql://liga-cluster.postgres.database.azure.com:5432/liga?sslmode=require
# spring.datasource.username=liga
# spring.datasource.password=Nikolamarvel123!